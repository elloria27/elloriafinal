
{
  "types": [
    "CREATE TYPE user_role AS ENUM ('admin', 'client', 'manager');",
    "CREATE TYPE component_status AS ENUM ('draft', 'published', 'archived');",
    "CREATE TYPE form_type AS ENUM ('contact', 'business', 'consultation');",
    "CREATE TYPE form_status AS ENUM ('new', 'in_progress', 'completed', 'cancelled');",
    "CREATE TYPE post_status AS ENUM ('draft', 'published', 'archived');",
    "CREATE TYPE referral_status AS ENUM ('pending', 'completed', 'cancelled');",
    "CREATE TYPE page_view_type AS ENUM ('page_view', 'conversion');",
    "CREATE TYPE expense_status AS ENUM ('pending', 'approved', 'rejected');",
    "CREATE TYPE expense_payment_method AS ENUM ('cash', 'credit_card', 'bank_transfer');",
    "CREATE TYPE expense_category AS ENUM ('office_supplies', 'marketing', 'utilities', 'other');",
    "CREATE TYPE supported_currency AS ENUM ('USD', 'CAD', 'EUR', 'GBP');",
    "CREATE TYPE supported_language AS ENUM ('en', 'fr', 'es');",
    "CREATE TYPE payment_status AS ENUM ('pending', 'completed', 'failed', 'refunded');",
    "CREATE TYPE task_status AS ENUM ('todo', 'in_progress', 'done', 'cancelled');",
    "CREATE TYPE task_priority AS ENUM ('low', 'medium', 'high');",
    "CREATE TYPE task_category AS ENUM ('development', 'marketing', 'support', 'other');",
    "CREATE TYPE notification_type AS ENUM ('task_assigned', 'task_updated', 'task_completed');",
    "CREATE TYPE reminder_recurrence AS ENUM ('none', 'daily', 'weekly', 'monthly');"
  ],
  "tables": [
    "CREATE TABLE IF NOT EXISTS pages (\n  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  title TEXT NOT NULL,\n  slug TEXT NOT NULL,\n  content JSONB NOT NULL DEFAULT '[]'::jsonb,\n  is_published BOOLEAN DEFAULT false,\n  show_in_header BOOLEAN DEFAULT false,\n  show_in_footer BOOLEAN DEFAULT false,\n  content_blocks JSONB[] DEFAULT '{}'::jsonb[],\n  parent_id UUID REFERENCES pages(id),\n  menu_order INTEGER DEFAULT 0,\n  created_at TIMESTAMPTZ DEFAULT timezone('utc'::text, now()),\n  updated_at TIMESTAMPTZ DEFAULT timezone('utc'::text, now())\n);",
    "CREATE TABLE IF NOT EXISTS site_settings (\n  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  logo_url TEXT,\n  site_title TEXT NOT NULL DEFAULT 'My Website',\n  created_at TIMESTAMPTZ NOT NULL DEFAULT timezone('utc'::text, now()),\n  updated_at TIMESTAMPTZ NOT NULL DEFAULT timezone('utc'::text, now())\n);"
  ],
  "triggers": [
    "CREATE TRIGGER update_pages_updated_at BEFORE UPDATE ON pages FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();",
    "CREATE TRIGGER update_site_settings_updated_at BEFORE UPDATE ON site_settings FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();"
  ]
}
